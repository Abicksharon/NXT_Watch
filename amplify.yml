version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npx browserslist@latest --update-db
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
